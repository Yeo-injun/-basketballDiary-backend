spring:
  datasource:
    url: jdbc:mysql://basketball-diary.comugfwcpzzw.ap-northeast-2.rds.amazonaws.com/basketballDiaryTest
    username: admin
    password: Basket5199

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #     show_sql: true
        format_sql: true

mybatis:
 configuration:
  multiple-result-sets-enabled: false
  map-underscore-to-camel-case: true
  call-setters-on-nulls: true
  jdbc-type-for-null: varchar
  default-fetch-size: 500
 mapper-locations:
 - classpath:mvc/repository/**/*.xml

server:
  servlet:
    session:
      timeout: 60
      tracking-modes: cookie

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace