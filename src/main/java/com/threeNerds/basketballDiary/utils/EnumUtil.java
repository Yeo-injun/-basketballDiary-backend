package com.threeNerds.basketballDiary.utils;

public class EnumUtil {
    /**
     * Code값을 넣어서 Enum에서 해당 코드의 이름을 조회한다.
     * @return
     */
    public static String getNameMatchedCode (Class<? extends Enum> targetEnum, String code) {
        String codeName = "";
        // TODO Java8 문법 적용해서 Stream으로 처리 요망
        // enum클래스가 뭔지 확인하고
        // enum클래스 안의 열거형을 배열로 반환받아서
        // 반복문을 순회하며 열거형타입의 코드값과 현

        // ENUM 활용하기 : 참고 - https://techblog.woowahan.com/2527/
        return codeName;
    }
}
